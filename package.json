{
  "name": "video-scroll",
  "version": "0.2.0",
  "description": "",
  "main": "js/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack && webpack --minify && cp index.html build/ && cp index.yaml build/ && cp -r css/ build/ && cp -r videos/ build/",
    "watch": "webpack --watch"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gl-matrix": "^2.3.1",
    "webglet": "^0.3.0"
  },
  "devDependencies": {
    "babel-core": "^6.2.1",
    "babel-loader": "^6.2.0",
    "babel-preset-es2015": "^6.1.18",
    "webpack": "^1.12.9"
  }
}
